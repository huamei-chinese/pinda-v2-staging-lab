# 项目当前状态报告

## 生成时间

2026-06-14

## 当前目标

面向越南学生的中文拼音听打练习软件，核心训练方式：

```text
越南语提示 / 中文语音 -> 输入拼音或汉字 -> 分格判断 -> 显示汉字、拼音、越南语解释、词性
```

## 已完成模块

### 1. HSK 课程模块

当前覆盖：

- HSK2
- HSK3
- HSK4
- HSK5

已完成能力：

- 课程等级入口。
- lesson 课程列表。
- 生词、短语、句子训练结构。
- 越译中模式。
- 听写模式。
- 分格拼音输入。
- 答对后显示汉字、拼音、越南语解释、词性。
- 错题记录、收藏、完成页。

### 2. 高频汉语模块

当前主题数量：12

每个主题训练项：

- 10 个生词
- 30 个短语
- 4 个句子

总训练项：528

当前主题：

- 问候与介绍
- 工作面试
- 课堂学习
- 旅游
- 点餐购物
- 出行交通
- 租房生活
- 生病与请假
- 职场生活
- 工厂车间
- 电商职场
- 亲密关系

### 3. UI 与交互

已完成：

- PC + 手机响应式基础适配。
- 语言切换入口。
- HSK / 高频汉语双模块入口。
- 练习页分格输入。
- 下划线提示防泄题。
- 右侧记忆卡防泄题。
- 底部功能按钮。
- 完成页。

### 4. 音频

已完成：

- 浏览器普通话 TTS 播放。
- 慢速播放。
- 优先选择 `zh-CN` / `zh-Hans` 普通话声音。
- 键盘输入音效。
- 正确音效。
- 错误音效。
- 完成奖励音效。

当前限制：

- 浏览器 TTS 最终音色取决于系统/浏览器安装的语音包。
- 自动测试可保证配置逻辑，最终听感仍需老板人工试听。

## 历史高风险 Bug 与当前状态

| 风险项 | 当前状态 |
| --- | --- |
| 未答前下划线底部提前显示提示 | 已修复，有测试 |
| 右侧记忆卡提前泄题 | 已修复，有测试 |
| `申请` 被拆成 `申/请` | 已修复，有测试 |
| `要求` 被拆成 `要/求` | 已修复，有测试 |
| 长句只有一条大下划线 | 已修复，有测试 |
| 常见动词被错误标成名词 | 已修复，有测试 |
| HSK5 出现 HSK2 低级功能词反复训练 | 已修复，有测试 |
| 慢速和普通速度差异不明显 | 已修复，有测试 |
| 浏览器优先选到非普通话中文声音 | 已修复，有测试 |

## 无人值守 QA 结果

### 高频内容生成

命令：

```bash
npm.cmd run generate:high-frequency
```

结果：

```text
topics: 12
items: 528
issueCount: 0
```

### 自动测试

命令：

```bash
npm.cmd test
```

结果：

```text
tests: 71
pass: 71
fail: 0
```

### 本地页面

地址：

```text
http://localhost:4173
```

结果：

```text
STATUS=200
```

## 饭后老板需要亲自验收的内容

自动化无法完全替代以下项目：

1. UI 审美打分。
2. 越南语是否足够地道。
3. 普通话 TTS 音色是否能接受。
4. 键盘音效、正确音效、错误音效是否舒服。
5. 长句练习时体感是否顺滑。

## 推荐下一阶段

如果老板验收达到 70 分以上：

1. 做最终 UI 小修。
2. 准备部署或打包。
3. 准备后续真实素材替换流程。

如果老板验收低于 70 分：

1. 优先修 UI 观感。
2. 再修音频听感。
3. 最后做内容微调。
